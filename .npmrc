# Hoisted layout = flat node_modules like npm — no symlinks.
# Required for Next.js standalone output tracing to correctly include
# native deps (@node-rs/argon2, better-sqlite3) in the Docker runtime image.
node-linker=hoisted
